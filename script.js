// regitration
  function registerUser(event) {
        // Prevent the default link navigation until registration is processed
        event.preventDefault();
        
        // Begin registration process
        // Replace the alert with your actual registration process.
        alert("Registration process started");

        // After the registration process is complete
        // You can then redirect to planner.html or any other page.
        window.location.href = "planner.html";
      }